#if USING_VORTEX_ITEMS
namespace Vortex.Sdk.InventorySystem.Model
{
    /// <summary>
    /// Что делать с количеством, не поместившимся в существующую пачку целиком. Отказ в приёме
    /// политикой не описывается — он приходит только от верификаторов вместимости.
    /// </summary>
    public enum StackPolicy
    {
        /// <summary>Добить существующие пачки до максимума, остаток положить отдельной пачкой.</summary>
        Merge = 0,

        /// <summary>Не дробить: не влезло в существующую пачку целиком — лечь отдельной пачкой.</summary>
        Whole = 1,

        /// <summary>Не складывать вовсе, каждый предмет отдельным экземпляром.</summary>
        None = 2,
    }
}
#endif
